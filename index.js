import  Express  from 'express'
import dotenv from 'dotenv'
import { bootstrap } from './bootsratp.js'
import con from './connections/DBconnection.js'
dotenv.config()
const app = Express(
const port = process.env.PORT
con
bootstrap(app)

